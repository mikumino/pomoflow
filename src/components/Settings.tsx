const Settings = () => {
    return (
        <dialog id="settings" className="modal">
            <div className="modal-box">
                <h1>chat</h1>
            </div>
        </dialog>
    );
}

export default Settings;